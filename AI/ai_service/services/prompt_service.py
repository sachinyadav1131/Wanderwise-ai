class PromptService:
    def get_template(self, name: str) -> str:
        # Placeholder for loading prompt templates from file systems (Phase 6+)
        return f"Mock Template for {name}"

prompt_service = PromptService()
